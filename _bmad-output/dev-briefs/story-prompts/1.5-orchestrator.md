Create scripts/run_pipeline.py implementing Story 1.5: Pipeline Orchestrator and Smoke Test.

Implementation:
- Import and call in sequence: discover, download, transcribe, embed (from yt_kg modules)
- Each stage call is wrapped in try/except so one stage crashing doesn't skip the rest
- After all stages, run a smoke test vector search:
  - Open LanceDB at "data/vectors.lance", table "chunks"
  - Embed the query "posterior chain exercises" with SentenceTransformer("BAAI/bge-small-en-v1.5") on CPU
  - Search top 5 results
  - Print each result as: https://youtu.be/{video_id}?t={int(start)}

Entry point: `if __name__ == "__main__": main()`

The file should sit at scripts/run_pipeline.py. Add __init__.py to scripts/ if needed for imports.
Use logging.basicConfig(level=logging.INFO) so progress is visible.
Do not modify any existing files.
